<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/ic_more_settings_24dp"
    android:minHeight="35dp">

    <TextView
        android:id="@+id/attendance_summary_subitem_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginLeft="20dp"
        android:layout_marginStart="20dp"
        android:text="@string/app_name"
        android:textSize="14sp" />

    <TextView
        android:id="@+id/attendance_summary_subitem_value"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:layout_marginEnd="25dp"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="25dp"
        android:layout_marginStart="10dp"
        android:layout_toEndOf="@id/attendance_summary_subitem_name"
        android:layout_toRightOf="@id/attendance_summary_subitem_name"
        android:gravity="end"
        android:text="@string/app_name"
        android:textSize="12sp" />
</RelativeLayout>
