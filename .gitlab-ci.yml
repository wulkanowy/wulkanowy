image: circleci/android:api-25-alpha

cache:
  paths:
     - .gradle/
     - /opt/android-sdk-linux/licenses/
  key: "$CI_COMMIT_REF_NAME"

build:
  stage: build
  script:
    - ./gradlew --no-daemon --stacktrace dependencies || true
    - ./gradlew --no-daemon --stacktrace assembleDebug
    - mv app/build/outputs/apk/app-debug.apk .
  artifacts:
    name: "${CI_PROJECT_NAME}_${CI_BUILD_REF_NAME}-${CI_BUILD_ID}"
    paths:
    - app-debug.apk

tests:
  stage: test
  script:
    - ./gradlew --no-daemon --stacktrace --debug test
    - ./gradlew --no-daemon --stacktrace --debug check
  artifacts:
    paths:
    - app/build/reports
